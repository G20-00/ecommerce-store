apiVersion : apps/v1
kind: Deployment
metadata:
  name: gecommercestore 
spec:
  replicas: 1
  selector:
    matchLabels:
      app: gecommercestore
  template:
    metadata:
      labels:
        app: gecommercestore 
    spec:
      containers:
        - name: gecommercestore 
          image: crecommerceauto.azurecr.io/gecommercestore
          ports:
          - containerPort: 3001
          env:
            - name: NODE_ENV
              value: production
            - name: NEXT_PUBLIC_API_URL
              value: "http://172.171.173.163:3000/api/74ded8ec-77d1-4390-bd27-36c7db2de037"
            - name: REACT_EDITOR
              value: atom